---
provenance: agent-synthesis
authored: 2026-06-04
---

# Campaign evaluation — typed-edge-predicate-ontologies

Persisted by the orchestrator from the **isolated-context evaluator** (ARD `evaluate`, FR.1 fence).
The evaluator assessed the **first draft** of `parent.md` against the seed using ONLY the synthesis
output + the seed — it did not read the specialist briefs, attestations, or decomposition rationale
(isolation enforced by what it was handed). It recorded, rather than acted on, every question that
would have required opening a brief.

## Verdict: APPROVED (advisory recommendations folded into the revision)

## Five-component assessment

1. **Coverage — strong.** Addresses all five seed facets (CiTO, IBIS, Toulmin, SKOS, substrate-
   native), differentiating each tradition's contribution. *Flagged:* the headline "twelve / ten /
   two" count was asserted, not shown — a reader couldn't reconstruct the 10/2 partition from prose.
2. **Coherence — holds together.** Shape → per-tradition → seam → bracket-framing answer, with the
   cross-facet join handled deliberately. *Flagged:* a presentation-order cost — `supports` appears
   under CiTO and under IBIS before the seam section reconciles it.
3. **Contradictions — present, substantive, honestly typed.** A real `## Contradictions` section
   that correctly types the dual-ancestry seam as `tension` (not `contradicts`) and explains why
   version/edition differences are evolution, not disagreement. Strengthened by a separate
   `## Disconfirming analysis` section.
4. **Groundedness — disciplined posture.** Every tradition claim carries a `[handle]{N}`; the
   `{inferred}` markers are used precisely at the composed cross-facet points (not laundering source
   claims); the secondary-source limitation is self-disclosed. *Flagged:* the bare numerics
   ("twelve / ten / two", "23 vs 70+") carried no handle/marker — composed-but-unmarked.
5. **Recommendations (advisory):** ground or mark the numerics; show the 10/2 partition as a
   predicate→ancestor table; forward-reference the seam at first mention of `supports`.

## Orchestrator note — disposition

This evaluation (APPROVED) and the parallel `adversarial-read` pass (NEEDS-REVISION — two provenance
errors: a misstated `contrasts` ancestor and an unsupported "both specialists" attribution) both ran
on the first draft. The NEEDS-REVISION triggered one revision pass, which fixed the two provenance
errors **and** incorporated all of this evaluator's advisory recommendations (the predicate→ancestor
table now grounds the 10/2 count; the numerics and 23/70+ counts are cited; the `contrasts` partial
ancestor is stated correctly). Post-revision: lint clean (0 broken / 0 thin); the lead spot-check
confirmed the fixes against `catalogs.json`. No second-pass NEEDS-REVISION — the revision is surgical
and verified, so it was not looped back to the user.
