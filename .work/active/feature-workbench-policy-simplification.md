---
id: feature-workbench-policy-simplification
kind: feature
status: active
created: 2026-09-19
updated: 2026-09-19
---
# Simplify Workbench activation and policy ownership

Apply the user-approved whole-Workbench simplification recommendations: remove the
session hook and its registration/tests, retain the managed AGENTS.md contract,
centralize lifecycle and Git preservation rules, move shared foundation guidance
out of setup, and make delegation-only and validator-authoring reading conditional.
Do not weaken closure, history preservation, verification, review, scope or adoption
requirements. No new enforcement, schema, receipts, or counters.

Keep ordinary work independent of setup references. Use a focused shared foundation
authoring reference under work alongside foundation reconciliation, rather than
making either reference oversized. Setup links outward to those shared rules.
Preserve the explicit greenfield-only principle-candidate link from ideate.

Without the hook, explain the optional CLAUDE.md projection's practical importance
for Claude Code; do not assume compaction behavior was tested or claim hosts load
instructions identically. Preserve user choice over setup and host projection.

Verify link destinations and anchors, managed block synchronization, manifest
references/parity, absence of live hook references, and the existing Workbench
tests. Walk through inline delivery, delegated delivery, setup and resumed closure.
Use one inline implementation review; the accepted direction needs no additional
design-review round. Update PR #64 only; no merge, version bump or publication.
Preserve this work record in a commit before trimming it.

## Delivery evidence

Removed the static session hook, its Codex manifest registration, and its dedicated
151-line test file. Kept a compact synchronized managed AGENTS.md block, including
the useful review-reference pointer. Documented instruction loading without a
hook and the optional CLAUDE.md projection; this repository already has that symlink.

Moved the shared document/engineering contract to work/references/foundation-authoring.md;
ordinary reconciliation stays in foundation-truth.md. Ordinary work no longer links
to setup references. Setup links to the shared contract, lifecycle, and existing
posture owners instead of duplicating them. Changed references remain at most 200
lines. Lifecycle now separates stale-state judgment from a five-step closing sequence,
retaining all acceptance and pre-trim Git obligations. Inline delivery can skip
assignment guidance; validator-author details are explicitly conditional.

Verification: 82 Workbench script tests pass; 175 changed-document links and anchors,
portable skill frontmatter, reference lengths, managed-block synchronization and
three-channel manifest version parity pass. No live references to removed hook files
or moved anchors remain. Research lint, index freshness and diff checks pass. Ledger
validation passes in an isolated copy excluding only the pre-existing untracked
.work/bin; the original remains untouched.

One focused inline implementation review checked preserved closure/history/scope
rules, shared foundation authority, entry-point reading conditions and hook removal
across metadata/docs. Corrected one stale link label. Walkthroughs (not live agent
runs) covered inline delivery, assigned delivery, greenfield setup and resumed
closure. No claims about host compaction behavior were made or tested.

Ready for PR #64 review. Preserve this record in the implementation commit before
trimming. No version bump, merge, publishing, or repository-local execution-default
change is included; the user's new inline-first preference discussion remains in chat.
