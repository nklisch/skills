---
id: idea-changelog-research-fields-at-bump
created: 2026-06-04
tags: [documentation]
---

gate-docs (pre-merge, 2026-06-04) Medium, deferred-to-release: when the
agile-workflow plugin is next version-bumped to ship the handoff-live-fields
work, its `CHANGELOG.md` should gain an entry noting the `research_refs`/
`research_origin` `.work/` schema fields and the `--research-origin`/
`--research-refs` work-view query flags (currently top entry is v0.9.5 with no
mention). Not actionable pre-merge — shipping/version bumps happen via
`bump-version.sh` at release time per repo convention; fold this into that bump's
CHANGELOG entry. (agentic-research has no CHANGELOG, which is correct for its
experimental 0.1.0 debut.)
