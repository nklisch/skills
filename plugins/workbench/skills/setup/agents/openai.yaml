interface:
  display_name: "Workbench Setup"
  short_description: "Explicitly consolidate a repository into Workbench"
  default_prompt: "Use $setup because I want to consolidate this repository into Workbench."
policy:
  allow_implicit_invocation: true
