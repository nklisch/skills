interface:
  display_name: "Research Handoff"
  short_description: "Hand research into an adopted Workbench ledger"
  default_prompt: "Use $research-handoff to propose items in this adopted Workbench repository."
policy:
  allow_implicit_invocation: true
