interface:
  display_name: "Release"
  short_description: "Summarize and clean completed Workbench outcomes"
  default_prompt: "Use $release to summarize completed Workbench outcomes and clean their retained files."
policy:
  allow_implicit_invocation: true
