interface:
  display_name: "Ideate"
  short_description: "Clarify uncertain outcomes in adopted Workbench"
  default_prompt: "Use $ideate to clarify this adopted Workbench outcome before creating work."
policy:
  allow_implicit_invocation: true
