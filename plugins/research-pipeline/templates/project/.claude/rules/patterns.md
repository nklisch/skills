# Patterns

Established code patterns in this repo. Follow these when writing new code.

When you need the full context for a pattern, use `/patterns <name>` to load it. To discover and document new patterns, use `/extract-patterns`.
